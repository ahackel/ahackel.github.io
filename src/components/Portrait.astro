---
export interface Props {
	src: string;
}

const { src, useShader = false } = Astro.props;
---
<img class="portrait" src={src} />

<style is:global>
    img.portrait {
        max-width: 20rem;
        margin: 0 auto;
        display: block;
        border-radius: 100%;
    }
</style>